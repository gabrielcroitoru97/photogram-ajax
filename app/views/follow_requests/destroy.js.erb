console.log("Un- request Follow request created");

// Update the button or UI to reflect the new state
$("#unrequest-button-<%= @follow_request.recipient_id %>").html("<%= j render partial: 'follow_requests/form', locals: { follow_request: @follow_request } %>");

