console.log("Follow request created for recipient <%= @follow_request.recipient_id %>");

// Update the button or UI to reflect the new state
$("#follow-button-<%= @follow_request.recipient_id %>").html("<%= j render partial: 'follow_requests/unrequest_button', locals: { follow_request: @follow_request } %>");
